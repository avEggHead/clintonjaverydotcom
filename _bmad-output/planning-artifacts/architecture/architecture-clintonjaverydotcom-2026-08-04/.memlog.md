---
scope: clintonjavery.com reimagining — full site architecture at feature altitude
purpose: build-substrate
altitude: feature
updated: 2026-08-05T20:53
---

- (decision) Fork1=A: Vite-native content pipeline (import.meta.glob eager + gray-matter + @mdx-js/rollup); no new orchestrator. Prevents a runtime-fetch/CMS divergence.
- (decision) Fork2=B: single custom Vite plugin owns content parse + mandatory-alt validation (FR-8) + per-post meta/sitemap/robots emission; build = tsc -b && vite build. Prevents invalid posts shipping + missing SEO.
- (decision) Fork3: Tailwind v4 via @tailwindcss/vite + @import tailwindcss + @theme tokens from DESIGN.md; CSS Modules retired per-component; build-time AA contrast gate on token pairs (closes UX open item).
- (decision) Fork4: Cloudflare Pages static deploy (retire Azure Storage/CDN workflow); public/_redirects with historic 301s first then /* /index.html 200 SPA fallback last.
- (constraint) Brownfield ratified: React19/RR7/Vite6-SWC/TS5.7 strict; Tailwind v4 dep present but unwired; 16 essays in src/data/posts.tsx string content; deploy = Azure Storage+CDN via GH Actions (to be retired); no _redirects, no tests.
- (event) spine finalized (status: final) — build-substrate, feature altitude, 7 ADs; lint_spine.py clean (0 findings)
